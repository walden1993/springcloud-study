eureka:
   client:
      serviceUrl:
         defaultZone: http://localhost:7999/eureka/
spring:
  cloud:
    config:
      label: master
      name: neo-config
      profile: dev
      discovery:
         enabled: true
         serviceId: springcloud-config-server       
  
    
